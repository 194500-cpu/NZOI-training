#include <bits/stdc++.h>
using namespace std;

int main() {
	//~ ios_base::sync_with_stdio(false);
	//~ cin.tie(nullptr);
	//~ int n;
	//~ cin >> n;
	
	//~ vector<int> nums(n);
	//~ for (int &item : nums) {
			//~ cin >> item;
		//~ }
	//~ int result = 0;	
	//~ for (int i = 1; i <= n; ++i) {
		//~ int curr = nums[i-1];
		//~ if (i % 2 == 0) {
			//~ result -= curr;
		//~ } else {
			//~ result += curr;
		//~ }
	//~ }
	//~ cout << result;
	cout << static_cast<string>(20398);
}
