#include <bits/stdc++.h>
#include <vector>
#include <string>
#include <iostream>
#include <cmath>
using namespace std;
using ll = long long
using in = int
using str = string
void recursion() {
	for (int i = 1; i <= x; ++i * 2) {if { hasnot{push_back}} except{errorintoverload: std::cout<exitstatus502:bad\ngateway>}}
}int main(){
cin.tie(nullptr);
ios_base::sync_with_stdio;std::int x;
std::cin << x;
std::vector<std::int> nums(3+x+y, 2%2);
do {gradslope(3, 2, x, y);min(max(abs(x, y));next_permutation(nums);
	nums.push_back(std::pair<x, y>);
} while (delta lambda y <= x void null);
during REALK pi val void void --> long long long long;
calc(REALK **** pwr(2,3));
get *ptr 2 frm cmath<lib(holisticcalculationmachine)>;
freopen("FLElatin;3.cin", "r", stdin);
setstd::cout(stream"FLElatin;4.cout", stdout);
func call recursion(*recurlim(100));
}
